#!/usr/bin/env bash

adduser --system \
  --group input \
  --quiet \
  --disabled-login \
  --no-create-home \
  {{name}}
systemctl daemon-reload
systemctl enable {{name}}.service
service {{name}} restart || service {{name}} start
