#!/usr/bin/env bash

adduser --system \
  --quiet \
  --disabled-login \
  --no-create-home \
  {{name}}
adduser {{name}} audio
adduser {{name}} input

systemctl daemon-reload
systemctl enable {{name}}.service
service {{name}} restart || service {{name}} start
