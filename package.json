{
  "name": "tot-tunes",
  "version": "0.0.0",
  "description": "Tot Tunes 🎵",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "test": "teenytest",
    "build": "scripty",
    "deploy": "scripty",
    "ship-it": "scripty",
    "debian": "scripty",
    "debian:template": "scripty",
    "deploy:deprecated": "ssh pi@raspberrypi.local 'sudo systemctl stop tot-tunes.service' && scp out/tot-tunes pi@raspberrypi.local:~/ && ssh pi@raspberrypi.local 'sudo systemctl start tot-tunes.service'",
    "build-deploy:deprecated": "npm run build:deprecated && npm run deploy:deprecated",
    "test:deprecated": "KEYBOARD_DEVICE=sample-data.bin node index.js",
    "build:deprecated": "pkg package.json -t node10-linux-armv7 --no-bytecode --out-path out"
  },
  "author": "Kaleb Lape",
  "license": "MIT",
  "teenytest": {
    "plugins": [
      "teenytest-promise"
    ]
  },
  "devDependencies": {
    "esbuild": "^0.4.3",
    "mustache": "^4.0.1",
    "prettier-standard": "^8.0.1",
    "scripty": "^2.0.0",
    "standard": "^12.0.1",
    "teenytest": "^5.2.0",
    "teenytest-promise": "^1.0.0",
    "testdouble": "^3.12.3"
  },
  "dependencies": {
    "@cycle/run": "^5.4.0",
    "polo": "^0.8.1",
    "xstream": "^11.12.0"
  }
}
